<!doctype html>
<html lang="{{ request.locale.iso_code | default: shop.locale | default: 'en' }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ page_title }}</title>
    {{ content_for_header }}
    {{ 'base.css' | asset_url | stylesheet_tag }}
  </head>
  <body style="background-color: {{ settings.background }};">
    <main id="MainContent" role="main">
      {{ content_for_layout }}
    </main>
  </body>
</html>
